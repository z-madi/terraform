```
terraform workspace list
terraform workspace new dev #(create the dev wp and switch into)
terraform workspace init
terraform workspace apply --auto-approve
terraform workspace select default
terraform workspace init
terraform workspace apply --auto-approve